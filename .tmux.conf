# change the prefix from 'C-b' to 'C-a'
# (remap capslock to CTRL for easy access)
unbind-key C-b
set-option -g prefix C-h
bind-key C-h send-prefix

# start with window 1 (instead of 0)
set-option -g base-index 1

# start with pane 1
set-option -g pane-base-index 1

# split panes using | and -
bind-key \ split-window -h
bind-key - split-window -v
unbind-key '"'
unbind-key %

# reload config file
bind-key r source-file ~/.tmux.conf

unbind-key p
bind-key p previous-window

# shorten command delay
set-option -sg escape-time 1

# don't rename windows automatically
set-option -g allow-rename off

# mouse control (clickable windows, panes, resizable panes)
set-option -g mouse on

# Use Alt-arrow keys without prefix key to switch panes
bind-key -n M-Left select-pane -L
bind-key -n M-Right select-pane -R
bind-key -n M-Up select-pane -U
bind-key -n M-Down select-pane -D

# enable vi mode keys
set-option -gw mode-keys vi
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-selection

# set-option default terminal mode to 256 colors
set-option -g default-terminal "screen-255color"

# fix pbcopy/pbpaste
if 'command -v reattach-to-user-namespace >/dev/null' \
    'set-option -gq @osx-pasteboard true'

if 'tmux show -gv @osx-clipboard' \
    'set-option -g default-command "reattach-to-user-namespace -l $SHELL"'

# present a menu of URLs to open from the visible pane. sweet.
bind-key u capture-pane \;\
    save-buffer /tmp/tmux-buffer \;\
    split-window -l 10 "urlview /tmp/tmux-buffer"


######################
### DESIGN CHANGES ###
######################

# panes
set-option -g pane-border-fg black
set-option -g pane-active-border-fg brightred

## Status bar design
# status line
set-option -g status-utf8 on
set-option -g status-justify left
set-option -g status-bg default
set-option -g status-fg colour12
set-option -g status-interval 2

# messaging
set-option -g message-fg black
set-option -g message-bg yellow
set-option -g message-command-fg blue
set-option -g message-command-bg black

#window mode
set-option -gw mode-bg colour6
set-option -gw mode-fg colour0

# window status
set-option -gw window-status-format " #F#I:#W#F "
set-option -gw window-status-current-format " #F#I:#W#F "
set-option -gw window-status-format "#[fg=magenta]#[bg=black] #I #[bg=cyan]#[fg=colour8] #W "
set-option -gw window-status-current-format "#[bg=brightmagenta]#[fg=colour8] #I #[fg=colour8]#[bg=colour14] #W "
set-option -gw window-status-current-bg colour0
set-option -gw window-status-current-fg colour11
set-option -gw window-status-current-attr dim
set-option -gw window-status-bg green
set-option -gw window-status-fg black
set-option -gw window-status-attr reverse

# Info on left (I don't have a session display for now)
set-option -g status-left ''

# loud or quiet?
set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off
set-option -gw monitor-activity off
set-option -g bell-action none

set-option -g default-terminal "screen-256color"

# The modes {
set-option -gw clock-mode-colour colour135
set-option -gw mode-attr bold
set-option -gw mode-fg colour196
set-option -gw mode-bg colour238

# }
# The panes {

set-option -g pane-border-bg colour0
set-option -g pane-border-fg colour19
set-option -g pane-active-border-bg colour0
set-option -g pane-active-border-fg colour9

# }
# The statusbar {

set-option -g status-position bottom
set-option -g status-bg colour18
set-option -g status-fg colour137
set-option -g status-attr dim
set-option -g status-left ''
set-option -g status-right '#[fg=colour233,bg=colour19,bold] %d/%m #[fg=colour233,bg=colour8,bold] %H:%M:%S '
set-option -g status-right-length 50
set-option -g status-left-length 20

set-option -gw window-status-current-fg colour1
set-option -gw window-status-current-bg colour19
set-option -gw window-status-current-attr bold
set-option -gw window-status-current-format ' #I#[fg=colour249]:#[fg=colour255]#W#[fg=colour249]#F '

set-option -gw window-status-fg colour9
set-option -gw window-status-bg colour18
set-option -gw window-status-attr none
set-option -gw window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

set-option -gw window-status-bell-attr bold
set-option -gw window-status-bell-fg colour255
set-option -gw window-status-bell-bg colour1

# }
# The messages {

set-option -g message-attr bold
set-option -g message-fg colour232
set-option -g message-bg colour16

# }

# Vim airline integration {

if-shell "test -f .tmux.snap" "source .tmux.snap"

# }
